Условие: Если есть 's, но нет Author, Christian, Beatrix:
    1. nameInCheck вырезается из subjectName от 0 до позиции амперсанта;
    2. Если nameInCheck нет в presRoles.Obj, в listOfCheckboxes добавляется чекбокс с nameInCheck.
    3. nameInCheck добавляется в presRoles.Obj.
Иначе: Если в presRoles.Obj нет subjectName:
    1. subjectName туда добавляется.
    2. Switch (Snake, mrs Jakins) - так и остается.
    3. То, что в else в default: в listOfCheckboxes добавляется чекбокс с subjectName.